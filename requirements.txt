# Core optional dependencies for enhanced features
# (Application will work without these, but with reduced functionality)

# ML clustering support (optional - only for clustering ml_model)
scikit-learn>=1.3.0

# Progress bars (optional - nice to have for visual feedback)
tqdm>=4.66.0

# NumPy (dependency of scikit-learn, optional)
numpy>=1.24.0